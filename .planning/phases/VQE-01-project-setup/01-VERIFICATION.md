# Phase 1: Project Setup - Verification

## Release Criteria

- [ ] Next.js app runs locally without errors.
- [ ] Supabase local instance is running and migrations are applied.
- [ ] Tailwind CSS is correctly rendering on the page.

## Verification Steps

1. Run `npm run dev` and navigate to `http://localhost:3000`.
2. Ensure the text "Marketnera QR Entry System" appears styled.
3. Check Supabase studio locally at `http://localhost:54323` to verify tables exist.
